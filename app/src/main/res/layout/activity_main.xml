<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:mapbox="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Vista del mapa -->
    <com.mapbox.maps.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <!-- Botón circular para centrar en la ubicación actual -->
    <Button
        android:id="@+id/btnCentrarUbicacion"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:text="📍"
        android:textSize="20sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:elevation="8dp"
        android:textColor="@android:color/white"
        android:background="@drawable/circular_button"/>

    <!-- Botón circular para ir a favoritos, a la izquierda -->
    <Button
        android:id="@+id/btnVerFavoritos"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:text="⭐"
        android:textSize="24sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="24dp"
        android:layout_marginStart="24dp"
        android:elevation="8dp"
        android:textColor="@android:color/white"
        android:background="@drawable/circular_button"/>

    <!-- Botón circular para cambiar estilo, a la derecha -->
    <Button
        android:id="@+id/btnCambiarEstilo"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:text="🎨"
        android:textSize="24sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="24dp"
        android:layout_marginEnd="24dp"
        android:elevation="8dp"
        android:textColor="@android:color/white"
        android:background="@drawable/circular_button"/>
</RelativeLayout>
